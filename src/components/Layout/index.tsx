import SearchList from '../SearchList'
import PositionButton from '../PositionButton'

const Layout = () => {
  return (
    <div>
      <PositionButton />
      <SearchList />
    </div>
  )
}

export default Layout